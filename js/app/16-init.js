/* =========================================================================
   INIT
   ========================================================================= */
document.addEventListener('DOMContentLoaded', ()=>{
  renderCampaignBar();
  wireCampaignBar();
  renderTabNav();
  renderActive();
  renderNoteSection();
  renderLogSection();
});
